const express = require('express');
const authenticationCtl = require('../controllers/authenticationCtl')
const router = express.Router();


//router.post('/createuser', authenticationCtl.createuser);

module.exports = router